[pytest]
# make the repo root importable so tests can `import app...` regardless of
# how pytest is invoked (bare `pytest` vs `python -m pytest`, local vs CI)
pythonpath = .
testpaths = tests
asyncio_mode = auto
