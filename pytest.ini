[pytest]
testpaths = tests
asyncio_mode = auto
# test_agent.py is a manual HTTP integration script that needs a live server;
# exclude it from the default (service-free) unit-test run.
addopts = --ignore=tests/test_agent.py
